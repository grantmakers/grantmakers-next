{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev --port 8787",
    "prod": "wrangler dev --port 8787 --remote",
    "deploy": "wrangler deploy",
    "mock-deploy": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "itty-router": "^4.0.26",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
